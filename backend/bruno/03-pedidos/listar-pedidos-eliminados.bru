meta {
  name: Listar pedidos eliminados
  type: http
  seq: 13
}

get {
  url: {{baseUrl}}/pedidos?limit=10&offset=0&eliminados=1
  body: none
  auth: none
}

assert {
  res.status: eq 200
}

docs {
  ## GET /pedidos?eliminados=1
  Solo soft-deleted (`eliminado_at` set). No mezclar con `estado`.
}
